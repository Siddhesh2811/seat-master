## Packages
framer-motion | Smooth animations for seat state transitions and layout
clsx | Conditional class merging
tailwind-merge | Class merging for components

## Notes
- Tailwind config should include `font-display` and `font-body` as custom font families.
- Seat IDs follow the format: `EVENTID-ZONE-SECTION-ROW-SEATNUM`
- Seat visualization relies on CSS Grid/Flex for responsive layouts.
- No authentication required for this admin view.
